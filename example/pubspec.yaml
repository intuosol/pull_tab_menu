name: pull_tab_menu_example
description: Example application for the pull_tab_menu package.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  pull_tab_menu:
    path: ../
  cupertino_icons: ^1.0.6
  material_symbols_icons: ^4.2808.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
